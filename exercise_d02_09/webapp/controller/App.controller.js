// Odata를 불러와 데이터 sort 를 통해 상위 5개만 불러 Line chart로 표현하는 예제

sap.ui.define(["sap/ui/core/mvc/Controller"], (BaseController) => {
  "use strict";

  return BaseController.extend("exercised0209.controller.App", {
    onInit() {},
  });
});
